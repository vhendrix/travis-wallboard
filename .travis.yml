language: node_js
node_js:
  - 0.12
before_script:
  - npm install -g karma-cli
  - export DISPLAY=:99.0
  - sh -e /etc/init.d/xvfb start
scripts:
- karma start karma.conf.js --single-run
branches:
  only:
    - master
    - develop
    - /^deploy-.*$/
